var listOfWords = [];
var wordCount = [];
var outputArray = [];

function listOfWords(word){
  if (wordList.indexOf(word, 0) == -1){
    listOfWords.push(word);
    wordCount.push(word: 1);
  } else {
    wordCount[word]++;
  }
  for (var i = 0; i < listOfWords.length; i++){
    var theWord = listOfWords[i];
    var theValue = wordCount[theWord];
  }
  return string(theWord), string(theValue);
};

function outCount()
